import { MultipleOfDirective } from './multiple-of.directive';

describe('MultipleOfDirective', () => {
  it('should create an instance', () => {
    const directive = new MultipleOfDirective();
    expect(directive).toBeTruthy();
  });
});
