import { RegexpValidatorDirective } from './regexp-validator.directive';

describe('RegexpValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexpValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
